# ========================================
# SISTEMA CONEXA - Variáveis para Coolify
# ========================================
# Copie e cole estas variáveis no painel do Coolify.
# Substitua os valores de exemplo pelos seus.
# ========================================

# ==========================================
# CONFIGURAÇÃO DO BANCO DE DADOS (PostgreSQL)
# ==========================================
# Use a string de conexão fornecida pelo Coolify.
DATABASE_URL=postgresql://user:password@postgres-host:5432/conexa_prod?schema=public

# ==========================================
# CONFIGURAÇÃO DO NODE.JS
# ==========================================
NODE_ENV=production
PORT=3001

# ==========================================
# SEGURANÇA (GERAR VALORES ÚNICOS)
# ==========================================
# Gerar com: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_USE_OPENSSL_RAND_BASE64_32
JWT_EXPIRES_IN=7d
SESSION_SECRET=CHANGE_ME_USE_OPENSSL_RAND_BASE64_32
BCRYPT_ROUNDS=12

# ==========================================
# CORS & URLs
# ==========================================
# Substitua pelo domínio real da sua aplicação no Coolify.
CORS_ORIGIN=https://conexa.seu-dominio.com

# ==========================================
# STORAGE (S3/R2) - OBRIGATÓRIO
# ==========================================
# Defina o tipo de storage: 's3' ou 'local'. Para produção, use 's3'.
STORAGE_TYPE=s3

# Nome do seu bucket no S3 ou R2.
BUCKET_NAME=conexa-uploads

# Região do bucket. Para R2, use 'auto'.
BUCKET_REGION=auto

# Endpoint da API. Essencial para Cloudflare R2.
# Exemplo R2: https://<account-id>.r2.cloudflarestorage.com
S3_ENDPOINT=https://<your-account-id>.r2.cloudflarestorage.com

# Forçar o estilo de caminho (path-style). Necessário para R2 e MinIO.
S3_FORCE_PATH_STYLE=true

# Suas credenciais de acesso.
ACCESS_KEY_ID=<your-r2-access-key-id>
SECRET_ACCESS_KEY=<your-r2-secret-access-key>

# ==========================================
# FEATURE FLAGS & SEED
# ==========================================
# Habilita a execução do `prisma db seed` no primeiro deploy.
PRISMA_SEED_ENABLED=true

# ==========================================
# OUTROS (OPCIONAL)
# ==========================================
OPENAI_API_KEY=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
